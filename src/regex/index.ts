import { CEP, CNPJ, CPF, plate } from "./BrazilianRegex";
import { onlyChars, onlyDigit } from "./GenericRegex";
import { username, email, http, hexColor } from "./WebRegex";

export default {
    CEP,
    CNPJ,
    CPF,
    plate,
    onlyChars,
    onlyDigit,
    username,
    email,
    http,
    hexColor,
};
