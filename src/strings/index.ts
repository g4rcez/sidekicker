import Cut from './unix/Cut';
import Tr from './unix/Tr';
import { decimals, cep, cpf } from './Format';
import {
  capitalize,
  titlelize,
  brazilize,
  convert,
  truncate,
  trueTrim,
  toInt,
  sneakize,
  slugify,
  rightPadding,
  leftPadding,
  reverse,
  camelize,
  readable,
  mask,
  onlyNumbers,
  nextChar,
  previousChar,
  padding,
  replaceAll,
  bothPadding,
} from './Transform';

export {
  titlelize,
  capitalize,
  brazilize,
  convert,
  truncate,
  trueTrim,
  toInt,
  sneakize,
  slugify,
  rightPadding,
  leftPadding,
  reverse,
  camelize,
  readable,
  mask,
  onlyNumbers,
  nextChar,
  previousChar,
  padding,
  replaceAll,
  bothPadding,
  decimals,
  cep,
  cpf,
  Cut,
  Tr,
};
